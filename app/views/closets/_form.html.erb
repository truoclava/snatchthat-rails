<%= form_for :closet, url: {action: 'create'} do |f|%>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.hidden_field :board_id, :value => @current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
